# 如何将 HTML 文件部署上线

仓库位置：`D:\我的github\Lovis-Log.github.io`

## 第一步：放入文件

在仓库里新建一个文件夹，把你的 HTML 和相关资源放进去：

```
D:\我的github\Lovis-Log.github.io\
├── 你的项目名\
│   ├── index.html        ← 页面
│   ├── style.css         ← 样式（可选）
│   └── 图片.png          ← 图片（可选）
```

> ✅ 也可以用已有的 `css/` `js/` `images/` 文件夹放公共资源。

## 第二步：更新导航页

编辑根目录的 `index.html`，在卡片列表里加一条链接：

```html
<div class="card">
  <a href="/你的项目名/">🔗 项目标题</a>
  <p class="desc">一句话描述</p>
</div>
```

## 第三步：提交并推送

在终端（或者 VSCode 的终端）里执行：

```bash
cd "D:\我的github\Lovis-Log.github.io"

git add -A
git commit -m "添加 xxx 项目"
git push
```

## 第四步：等待部署

推送后 GitHub 会自动部署，大概 1-2 分钟。

访问 `https://lovis-log.github.io/` 就能看到更新后的页面。

---

### 以后的操作总结

| 步骤 | 做什么 |
|------|--------|
| 1 | 把 HTML 放入新文件夹 |
| 2 | 在首页 `index.html` 加链接卡片 |
| 3 | `git add -A` → `git commit -m "说明"` → `git push` |
| 4 | 等 1-2 分钟，刷新网页 |
